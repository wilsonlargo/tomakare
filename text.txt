            return fromClsLibrerias.map(libro => {
                const LibreriaNew = new Libro(
                    libro.nombre,
                    libro.tipo,
                    libro.keys,
                    libro.link
                );
                return LibreriaNew;
            });